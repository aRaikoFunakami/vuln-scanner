# vuln-scanner

## バージョニング

`threats.json` に新しい脅威（脆弱パッケージ・サプライチェーン攻撃）を追加したら、
`pyproject.toml` の `version` をマイナーバージョンで1つ上げる（例: 1.1.0 → 1.2.0）。

- 新しい脅威の追加 → マイナーバージョンアップ
- 既存脅威のバージョン追加・バグ修正のみ → パッチバージョンアップ

バージョンを上げたら `uv lock` を実行して `uv.lock` も同期すること。

## レビューフェーズ（実装完了後に必ず実施）

機能実装・バグ修正が終わったら、コミット前に以下の観点でセルフレビューする。
過去の検出漏れ（pnpm v9 scoped / yarn berry / semver範囲 / 脅威追記による判定反転 /
終了コード / ネスト node_modules）はすべてこのいずれかの欠落が原因だった。

### 1. fixture は実ツールで生成する（手書き禁止）

- lockfile・node_modules 構造のテストデータは、実際の `npm install` / `yarn` /
  `pnpm install`（**現行バージョン**）の出力から作る。記憶や推測でフォーマットを書かない。
- 各フォーマットの複数世代（package-lock v1/v2/v3、yarn classic/berry、pnpm v6/v9）と
  scoped / 非scoped の両方を必ず含める。
- パーサを触ったら、実ツール生成の lockfile でパース結果の**件数**まで確認する。

### 2. 「検出できないこと」を検証する

- 検出系のテストは「見つかること」だけでなく「見つからない側」も赤くなるように書く：
  非空の依存ファイルからパース結果が 0 件なら、それは異常（解析失敗）として扱えるか。
- 「本当にクリーン」と「パーサが読めず0件」が同じ出力にならないかを確認する。
- ディスク走査は npm hoist / pnpm ストア（`.pnpm/`）/ ネストした `node_modules` の
  3配置すべてで検証する。

### 3. threats.json 追記の不変条件

- **脅威エントリを1件追加しても、既存の判定結果が一切変わらないこと。**
  無関係なダミー脅威を threats.json 末尾に追記 → 同じ fixture をスキャン →
  結果が完全一致することを確認する（判定は必ず全脅威横断の
  `vuln_scanner.threats.judge()` を経由させ、個別脅威の judge で上書きしない）。

### 4. 判定ロジック

- semver 範囲指定（`^` `~` `>=`）を固定バージョンとして SAFE 断定しない。
  範囲が脆弱版を含みうる場合は WARNING。
- 判定は「対象を見つけた後の分類」だけでなく「見つけられなかった」状態
  （未対応フォーマット等）を区別して出力する。

### 5. CLI 契約

- 終了コードをテストする：VULNERABLE 検出時は非0。CI ゲートとして使えること。
- 単体テストだけで終わらせず、実 fixture プロジェクトへの E2E スキャンを
  npm / python 各エコシステムで最低1本通す。
